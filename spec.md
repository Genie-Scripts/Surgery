# Surgery — 手術データダッシュボード 要件定義書 v0.1

執筆: 2026-04-30
状態: ドラフト（実装着手前のレビュー対象）

---

## 1. プロジェクト概要

### 1.1 目的
外科手術データから **術者評価** と **カテゴリ別分析** を可視化するダッシュボードを構築する。手術内容のばらつき・経時変化・同僚比較を自分自身で確認し、教育・研鑽・認定維持に活用する。

### 1.2 利用者
- **一次利用者**: 自分（執刀医本人）
- **配信形態**:
  - **ローカル実名版**: Mac 内、自分のみアクセス
  - **匿名化 web 版**: インターネット配信（公開先は §8.2 で要決定）

### 1.3 関連プロジェクト
本プロジェクトは隣接する以下プロジェクトと **完全に独立** に運用する：
- `/Users/genie/dev/ai-apps/Outpatient-Dashboard/` — 外来効率化ダッシュボード（経営企画室向け）
- `/Users/genie/dev/ai-apps/Outpatient-Restructuring/` — 外来枠再編診断

ただし、設計原則（後述 §6.2）と LLM クライアント構造は Outpatient-Dashboard から踏襲し、将来の統合・共通化の余地を残す。

---

## 2. データ仕様

### 2.1 入力 CSV
- **ファイル名**: `anonymized_data.csv` 同一スキーマで継続供給
- **文字コード**: 現サンプルは **UTF-8 BOM 付き（`utf-8-sig`）** で読込可能。Windows 出力経路で Shift-JIS / CP932 が混入する可能性は残るため、`pandas.read_csv` のフォールバック付き読込ユーティリティを実装する（`utf-8-sig` → `cp932` → `shift_jis` の順で試行）。
- **セル内改行**: `実施術者` `麻酔種別` `確定術式` `術後病名` の 4 列はセル内で `\r\n` または `\n` 改行による複数値連結あり。区切り文字は両方混在するため `re.split(r'\r?\n', s)` で正規化する。
- **更新頻度**: 週次もしくは不定期
- **実スキーマ（2026-04-30 サンプル: 1,091 行 × 14 列で確定 / OQ-1 解決済み）**:

  | # | 列名 | 型 | 欠損 | 備考 |
  |---|---|---|---|---|
  | 1 | `手術実施日` | str | 0 | `YYYY/MM/DD` |
  | 2 | `実施診療科` | str | 1 | 18 診療科 |
  | 3 | `実施手術室` | str | 0 | `OP-NN` 形式 |
  | 4 | `麻酔科関与` | str | 0 | `あり` / `なし` |
  | 5 | `入外区分` | str | 0 | `入院` / `外来` |
  | 6 | `申込区分` | str | 0 | `通常` / `臨時` / `緊急` ←spec 上の「緊急区分」相当 |
  | 7 | `実施術者` | str | 22 | **セル内改行で複数医師（執刀＋助手）連結**。先頭行=執刀医、2 行目以降=助手。匿名 ID `医師_NNN` |
  | 8 | `麻酔種別` | str | 624 | セル内改行で複数行。NaN は局所麻酔等で麻酔科記録外（≒`麻酔科関与=なし`） |
  | 9 | `入室時刻` | str | 0 | `HH:MM` |
  | 10 | `退室時刻` | str | 0 | `HH:MM` |
  | 11 | `予定手術時間` | int | 0 | **分**（15〜600） |
  | 12 | `予定手術時間(OR)` | str | 0 | `HH:MM`（`予定手術時間` と同値の表示用） |
  | 13 | `確定術式` | str | 1 | セル内改行で複数術式・加算が連結 |
  | 14 | `術後病名` | str | 13 | 同じく複数行 |

- **spec.md の旧「想定列」と差分**（注意）:
  - **K コード／術式コード列は無い** → OQ-2（難度補正）は外保連指数を直接使えない。データ供給元への追加要請または `確定術式` から K コード辞書を自前構築のいずれかが必要。
  - **退院日・受付日列は無い** → OQ-8 の月次基準は「手術実施日」一択（解決済み）。
  - **「術者 ID」「助手 ID」は分かれていない** → `実施術者` 1 列をセル内改行で分解する。

### 2.2 データ量
- 想定: **年間 15,000 件**
- 数年分蓄積で総件数 **数万 〜 10 万件規模**
- pandas 集計／LLM キャッシュで実用速度に収まる想定（§4.2 参照）

### 2.3 個人情報の扱い
- 入力 CSV 時点で **術者・患者は事前匿名化済み**（外部供給元で実施）
- 念のため、ローカルで保持する補助情報（実名↔匿名 ID 対応表など）は別系統で管理し、Git 管理から完全に隔離する
- 公開版（web）は匿名 ID と統計のみ表示、実名・カルテ番号は一切含めない

---

## 3. 機能要件

### 3.1 MVP（Phase 1）= A + B + C + D
| ID | 機能 | 内容 |
|---|---|---|
| **A** | CSV 読み込み + カテゴリ自動抽出 | regex 一次フィルタ → 曖昧症例のみ LLM 判定 |
| **B** | 術者別・診療科別 集計テーブル | KPI（§3.3）を表形式で表示 |
| **C** | 月次推移グラフ | 件数・平均手術時間などの時系列 |
| **D** | Streamlit ダッシュボード UI | A/B/C を統合した対話的閲覧画面 |

「ある程度動くもの」を最短で立ち上げ、**追加機能は実データ運用しながら判断**する方針。

### 3.2 抽出カテゴリ（MVP 固定）
1. **悪性腫瘍**
2. **人工関節**
3. **ロボット支援** — 2 サブカテゴリで保持
   - **3a. ダヴィンチ系**（保険上「内視鏡下手術用支援機器使用」加算系。一般消化器／泌尿器／産婦／呼吸器など）
   - **3b. 非ダヴィンチ系**（整形外科の人工関節ロボット等。`（ロボット）` 表記）

抽出ロジック（§7 で詳述）:
- **第1段（regex）**: 手術名・術式コードに含まれるキーワード／パターンで一次タグ付け
- **第2段（LLM）**: regex でヒットしない／曖昧／複合症例（例：「腹腔鏡下ロボット支援前立腺全摘」）に Swallow-8B が判定
- **第3段（手動）**: 検証用ラベリング UI（Phase 2 = E）で訂正、ground truth として再学習素材に

### 3.3 KPI 定義（術者評価）

3 階層の **同心円的な絞り込み** に加え、各レイヤーで **直交する 3 つの絞り込み軸** を提供する。

**直交絞り込み軸（全レイヤー共通）**:
| 軸 | 値 | 用途 |
|---|---|---|
| **執刀医モード** | `執刀医のみ` / `執刀医＋助手を含む` | `実施術者` セル内改行を分解し、先頭行=執刀医・2 行目以降=助手として、術者集計のスコープを切替（実データ: 執刀医ユニーク 109 名 / 全医師 137 名） |
| **申込区分** | `通常` / `臨時` / `緊急` / `全て` | `申込区分` 列で絞り込み。緊急症例比率は術者評価の独立指標 |
| **ロボット内訳** | `ダヴィンチ系のみ` / `非ダヴィンチ系のみ` / `両方` / `非ロボット` / `区別なし` | レイヤー 3 のロボット支援カテゴリで使用。診療科横断のダヴィンチ件数と整形外科限定の人工関節ロボット件数は別物として扱う |

#### レイヤー 1: 全データ（フィルタ無し）
| KPI | 計算 |
|---|---|
| 件数 | COUNT |
| 総手術時間 | SUM(`予定手術時間`)  ※単位は分 |
| 平均手術時間 | MEAN(`予定手術時間`) |
| 構成比（全術者中） | 自身件数 / 全件数 |
| 月次推移 | 月別 COUNT / MEAN（月定義は **手術実施日基準**、中途月は土日祝日除外の営業日換算 / OQ-8 解決済み） |
| 緊急症例比率 | `申込区分=緊急` / 全件数 |
| 同僚比較 | 同診療科の他術者との分布上での自身の位置 |
| 難度補正 | **OQ-2** 計算式は別途決定（K コード列が無いため要再検討） |

#### レイヤー 2: 全身麻酔手術のみ
- レイヤー 1 と同じ KPI セット
- フィルタ条件（OQ-5 解決済み）:
  ```python
  ("全身麻酔" in 麻酔種別) and ("20分以上" in 麻酔種別)
    or "全身麻酔(20分以上：吸入もしくは静脈麻酔薬)" in 麻酔種別
  ```
  - 実サンプルでは両条件等価で 406 件
  - `上下肢の伝達麻酔(全身麻酔時算定不可)` の誤検出を避けるため単純な「全身麻酔」マッチは禁止

#### レイヤー 3: 抽出カテゴリ別
- カテゴリ（悪性腫瘍／人工関節／ロボット支援）ごとに、期間ランキング等
- ロボット支援は **ダヴィンチ系 / 非ダヴィンチ系** の 2 サブカテゴリで集計（上記「ロボット内訳」軸で切替）
- **UI/UX は別途相談**（MVP 段階では基本的なテーブル表示まで）

### 3.4 将来拡張（Phase 2+）
| ID | 機能 | 優先 |
|---|---|---|
| E | 検証用ラベリング UI | 高（精度向上の前提） |
| F | レポート PDF 出力 | 中 |
| G | 自動定期更新 | 中（pull.sh / deploy.sh で代替可能） |
| - | カテゴリ追加（腹腔鏡／胸腔鏡／白内障 等） | 中（規模次第） |
| - | 難度補正の精緻化 | 中（OQ-2 解決後） |

---

## 4. 非機能要件

### 4.1 精度
- **MVP 目標**: カテゴリ自動判定 **>= 90%**（手動ラベリング検証ベース）
- 不達なら：曖昧症例の LLM 二次判定をより高品質モデル（Qwen2.5-32B 等）に切替、もしくはプロンプト調整
- 検証用 **手動ラベリング** リソースは別途確保可能（Phase 2 機能 E）

### 4.2 性能
- 数万 〜 10 万件 × 集計：pandas で数秒以内
- LLM 判定: regex 一次フィルタ通過分のみ → 1日あたり数十件想定 → Swallow-8B で 3〜5 秒/件 → 数分以内
- LLM 判定キャッシュ（手術名 → カテゴリ）で **2 回目以降は即時**

### 4.3 セキュリティ・データ取り扱い
Outpatient-Dashboard の鉄則を踏襲：
- `data/raw/` は **Git コミット厳禁**
- 補助対応表（もしあれば）は **Git コミット厳禁**
- ローカル実名版は `local/` ディレクトリ配下に隔離し `.gitignore` 対象
- 公開（web）は匿名版のみ
- 新規ファイル作成時は `.gitignore` 確認を徹底

---

## 5. 技術スタック

| レイヤー | 採用 | 備考 |
|---|---|---|
| 言語 | **Python 3.11+** | 既存資産・型ヒント前提 |
| UI | **Streamlit** | 最速で動くもの優先 |
| 集計 | pandas | |
| グラフ | Plotly（推奨）or Altair | Streamlit との親和性 |
| LLM ランタイム | Ollama（既導入） | OpenAI 互換 API 経由 |
| LLM モデル | **`hf.co/mmnga/Llama-3.1-Swallow-8B-Instruct-v0.5-gguf:Q6_K`** | Outpatient-Dashboard の Phase A.0 で実績あり：100% 採用率・3.4s/件・行混同型ソフトハルシは要注意 |
| データストア | CSV 直読み | Shift-JIS / UTF-8 自動判別 |
| LLM キャッシュ | JSON ファイル（sha256(name+rules+model)） | Outpatient-Dashboard の `data/llm_cache/` パターン踏襲 |
| テスト | pytest | tests/ 配下 |
| Lint / Format | Ruff / Black | pyproject.toml で設定 |

### 5.1 LLM モデル選定の根拠
直近の Outpatient-Dashboard Phase A.0（薬再診観察コメント）で：
- 365/365 件 LLM 採用（フォールバック 0）
- 平均 3.0s/件
- フォーマット規約遵守：100%
- 数値引用：正確
- 注意点：**行混同型ソフトハルシ**（異なる行の値を同じ文に並べてしまう）

カテゴリ判定は単一手術名→カテゴリ ID の単純タスクなので、行混同問題は発生しにくい想定。Swallow-8B Q6_K で十分実用域。

---

## 6. アーキテクチャ

### 6.1 データフロー
```
┌──────────────┐
│ 生データCSV   │ data/raw/  ※Gitコミット禁止
│ (Shift-JIS)  │
└──────┬───────┘
       ↓
┌──────────────┐
│ 文字コード正規化 │ src/ingest.py
│ (UTF-8化)     │
└──────┬───────┘
       ↓
┌──────────────────┐
│ カテゴリ抽出       │ src/classify.py
│ regex → LLM →   │ data/llm_cache/categories.json
│ (手動ラベリング) │
└──────┬───────────┘
       ↓
┌──────────────┐
│ 集計データ      │ data/aggregated/  ※Gitコミット可
│ (CSV/Parquet) │
└──────┬───────┘
       ↓
       ├─→ ┌──────────────────────┐
       │   │ Streamlit ダッシュボード│ app/main.py
       │   │ (ローカル実名版)        │ → local/ にレポート保存
       │   └──────────────────────┘
       │
       └─→ ┌──────────────────────┐
           │ 静的HTML出力（匿名版）   │ scripts/deploy.sh
           │                        │ → docs/ → GitHub Pages
           └──────────────────────┘
```

### 6.2 設計原則（Outpatient-Dashboard から踏襲）
1. **LLM には計算をさせない**: 数値処理・集計・評価は全て Python。LLM はカテゴリ判定の自然言語理解にのみ。
2. **集計データを標準インターフェースに**: 集計層と UI 層を疎結合に。生 CSV への直接アクセスは ingest / classify モジュールのみ。
3. **匿名化の一貫性**: 補助対応表（もしあれば）で同一術者は常に同一匿名 ID。
4. **静的成果物の優先**: 公開版は静的 HTML、ローカルは Streamlit、と用途で分離。

---

## 7. カテゴリ抽出ロジック（詳細設計の素案）

### 7.1 regex 第 1 段
`config/categories.yaml` に YAML で定義（編集容易性のため）:

```yaml
malignant_tumor:
  display_name: 悪性腫瘍
  patterns:
    - "癌"
    - "がん"
    - "ガン"
    - "悪性"
    - "腫瘍.*切除"  # 良性除外は LLM 二次判定で
  k_codes: ["K6...", "K7..."]  # 該当する診療報酬コード接頭辞

artificial_joint:
  display_name: 人工関節
  patterns:
    - "人工.*関節"
    - "T[HK]A"  # THA / TKA
    - "全人工股関節"
    - "全人工膝関節"
  k_codes: []

robot_assisted_davinci:
  display_name: ロボット支援（ダヴィンチ系）
  patterns:
    - "手術用支援"  # 「内視鏡下手術用支援機器使用」加算系。実サンプル 37 件
  k_codes: []

robot_assisted_other:
  display_name: ロボット支援（非ダヴィンチ系）
  patterns:
    - "ロボット"   # 整形外科の人工関節ロボット等。実サンプル 5 件（全て整形外科）
  exclude_patterns:
    - "手術用支援"        # ダヴィンチ系と二重カウントしないため
    - "画像等手術支援"    # ナビ加算は別物
  k_codes: []
```

OQ-6 解決済み（2026-04-30）: 当初候補の `ロボット支援` / `ロボット補助` / `ダヴィンチ` / `da Vinci` / `RA-` は実サンプルでは 0 件ヒット。保険請求名称ベースの `手術用支援` と整形外科表記の `（ロボット）` の 2 軸で確定。

### 7.2 LLM 第 2 段（曖昧症例）
- 第 1 段で **0 件ヒット** または **2 件以上ヒット**（カテゴリ重複）の症例を LLM 判定対象とする
- プロンプトは "手術名 + 術式コード + 麻酔方法 → どのカテゴリに該当するか（複数可）" の単純多ラベル分類
- 出力規約: `CATEGORIES: [malignant_tumor, robot_assisted]` の 1 行
- キャッシュキー: `sha256(手術名 + コード + ルールバージョン)`

### 7.3 手動ラベリング第 3 段（Phase 2）
- 検証 UI で「LLM が外したかもしれない症例」を抽出 → 訂正 → ground truth 蓄積
- 蓄積した ground truth はキャッシュより優先

---

## 8. 運用

### 8.1 自動化スクリプト（Outpatient-Dashboard と同パターン）
| スクリプト | 用途 |
|---|---|
| `scripts/pull.sh` | 定期データ取得（外部供給元の置き場から rsync 等） |
| `scripts/deploy.sh` | 静的 HTML を GitHub Pages 等へ push |
| `scripts/local_build.sh` | ローカル実名版を `local/` に生成（Streamlit 起動 or 静的書き出し） |

### 8.2 公開方法（要決定）
| 案 | 概要 | 利点 | 欠点 |
|---|---|---|---|
| **A. Streamlit Cloud** | 匿名版を Streamlit Community Cloud にデプロイ | 対話 UI そのまま公開 | 公開鯖のセキュリティ責任、無料枠制約 |
| **B. 静的 HTML + GitHub Pages** | 集計結果を静的 HTML レポート化、Pages で配信 | サーバ不要、Outpatient-Dashboard と同パターン | 対話性なし、生成コスト |
| **C. ハイブリッド** | ローカルは Streamlit、公開は静的 HTML | 用途分離 | 開発工数倍 |

**MVP は B（静的 HTML）から開始** を推奨：
- Streamlit と HTML の両方を書くほど初期工数を割けない
- Streamlit はローカル実名版で活用、公開は静的化で安全側に倒す
- Phase 2 で C に拡張可能

→ ただし MVP は A + B + C + D（D = Streamlit UI）なので、**ローカルは Streamlit、公開はオプション** と整理する。`deploy.sh` は Phase 2 で着手。

---

## 9. ディレクトリ構成

```
Surgery/
├── README.md                  # セットアップ手順
├── spec.md                    # 本ファイル（要件定義）
├── CLAUDE.md                  # Claude Code 向けプロジェクト指示書（Phase 1 着手時に追加）
├── pyproject.toml             # 依存・lint 設定
├── .python-version
├── .gitignore                 # data/raw, local/, master_key 等を除外
│
├── src/                       # ロジック本体
│   ├── __init__.py
│   ├── ingest.py              # CSV 読み込み・文字コード正規化
│   ├── classify.py            # カテゴリ抽出（regex + LLM）
│   ├── aggregate.py           # KPI 集計
│   ├── llm_client.py          # Ollama / OpenAI 互換クライアント（Outpatient から移植）
│   ├── cli.py                 # 統合 CLI
│   └── core/
│       ├── __init__.py
│       ├── kpi.py             # KPI 計算純関数
│       ├── peer_compare.py    # 同僚比較ロジック
│       └── difficulty.py      # 難度補正（OQ-2）
│
├── app/                       # Streamlit エントリ
│   ├── main.py                # streamlit run app/main.py
│   ├── pages/
│   │   ├── 1_全体KPI.py
│   │   ├── 2_術者別.py
│   │   ├── 3_カテゴリ別.py
│   │   └── 4_月次推移.py
│   └── components/            # 共通ウィジェット
│
├── config/
│   ├── categories.yaml        # カテゴリ抽出ルール
│   ├── llm_config.yaml        # Ollama 接続設定
│   └── peers.csv              # 同僚比較対象（必要に応じて）
│
├── data/
│   ├── raw/                   # 生データCSV ※Gitコミット禁止
│   ├── normalized/            # UTF-8 正規化済み
│   ├── aggregated/            # 集計結果
│   └── llm_cache/             # LLM 判定キャッシュ
│
├── docs/                      # 公開用静的HTML（Phase 2 で活用）
├── local/                     # ローカル実名版出力 ※Gitコミット禁止
│
├── scripts/
│   ├── pull.sh                # データ取得（Phase 2）
│   ├── deploy.sh              # 公開（Phase 2）
│   └── local_build.sh         # ローカル実名版ビルド
│
├── tests/
│   ├── fixtures/              # 小サンプルCSV
│   ├── test_ingest.py
│   ├── test_classify.py
│   └── test_aggregate.py
│
└── tools/                     # Phase 2 — ラベリング UI 等
```

---

## 10. 制約・前提
- 動作環境: 自分の MacBook Pro M5 Pro 64GB のみ
- アクセス制御: 不要（ローカル）／公開版は閲覧のみ
- 外部 API 課金: 発生させない（Ollama ローカルのみ）
- 既存 Outpatient-Dashboard / Outpatient-Restructuring とは独立リポジトリ
- 個人開発・自分用ユーティリティとしての実装精度（業務利用は当面 90% 程度）

---

## 11. 未決事項（Open Questions）

| ID | 内容 | 状態 / 期限 |
|---|---|---|
| **OQ-1** | 入力 CSV の **実スキーマ** 確認（列名・型・欠損パターン） | **解決済み（2026-04-30）** §2.1 に確定スキーマを記載。K コード列・退院日・受付日は無し |
| **OQ-2** | **難度補正** の計算式（K コードベース／手術時間中央値ベース／専門医試験基準 等） | Phase 1 後半。**前提変更**: K コード列が無いためデータ供給元への追加要請または `確定術式` から K コード辞書を自前構築のいずれかが必要 |
| **OQ-3** | **同僚比較** の対象集合 | Phase 1 中盤。**方針案**: 総件数／総手術時間／ロボット支援は全診療科横断、それ以外は診療科内 |
| **OQ-4** | **公開方法** 確定（A / B / C from §8.2） | Phase 2 着手時 |
| **OQ-5** | 「**全身麻酔**」の表記ゆれ列挙 | **解決済み（2026-04-30）** §3.3 レイヤー 2 に判定式を記載 |
| **OQ-6** | **ロボット支援** の判定キーワード | **解決済み（2026-04-30）** ダヴィンチ系（`手術用支援`）と非ダヴィンチ系（`（ロボット）`）の 2 サブカテゴリで確定。§3.2 / §7.1 参照 |
| **OQ-7** | カテゴリ自動判定の **正解ラベル**（手動ラベリング） 何件確保するか | Phase 1 後半 |
| **OQ-8** | 月次推移の **「月」定義**（手術日 / 退院日 / 受付日） | **解決済み（2026-04-30）** 退院日・受付日列が無いため `手術実施日` 一択。中途月は土日祝日除外の営業日換算（手術休止日マスタは `config/calendar.yaml` で別途管理） |

OQ-1・5・6・8 解決済み（実 CSV 確認）。残る検討事項は OQ-2 / OQ-3 / OQ-4 / OQ-7。

---

## 12. レビューチェックリスト

実装着手前に以下を確認：
- [x] OQ-1（実スキーマ）解決済み（2026-04-30）
- [x] OQ-5・OQ-6・OQ-8 解決済み（2026-04-30）
- [ ] `.gitignore` に `data/raw/`・`local/`・補助対応表が含まれている
- [ ] `config/categories.yaml` のカテゴリ定義初版が固まっている（ロボット 2 サブカテゴリ反映）
- [ ] LLM モデルが Ollama で pull 済み (`ollama list` で確認)
- [ ] サンプル 100 件で regex 第 1 段の精度ざっくり確認
